import styled from "@emotion/styled";

const OrderTableColStatusLinkStyled = styled.div`
`

export default function OrderTableColStatusLink({children}) {
    return (
        <OrderTableColStatusLinkStyled>{children}</OrderTableColStatusLinkStyled>
    )
}
import styled from "@emotion/styled";

const OrderColInputNumberBoxStyled = styled.div`
`

export default function OrderColInputNumberBox({children}) {
    return (
        <OrderColInputNumberBoxStyled>{children}</OrderColInputNumberBoxStyled>
    )
}